import React from "react";

const page = () => {
    return (
        <div>
            Sales and Performance Analytics: Monitor sales performance and
            analytics of routes and users. View sales history, revenue, and
            other relevant metrics. Generate reports and analytics dashboards
            for insights.
        </div>
    );
};

export default page;
